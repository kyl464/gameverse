import { useRouter } from 'next/router';

export default function GameTopUp() {
  const router = useRouter();
  const { gameId } = router.query;

  return (
    <div>
      <h1>Top-Up Game: {gameId}</h1>
      <form>{/* Form Top-Up di sini */}</form>
    </div>
  );
}
